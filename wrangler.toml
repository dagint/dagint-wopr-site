# Cloudflare Pages – DaGint site
# This file is for manual CLI deployments only (npx wrangler pages deploy dist)
# For Git-based deployments, Cloudflare Pages ignores this file
name = "dagint-site"
pages_build_output_dir = "./dist"
