# Copy to .env.local for local development. Do not commit .env or .env.local.
# In production, set these in Cloudflare Pages → Settings → Environment variables.

# Formspree form ID (from formspree.io form URL: .../f/YOUR_FORM_ID)
PUBLIC_FORMSPREE_FORM_ID=

# Cloudflare Turnstile site key (from Turnstile dashboard)
PUBLIC_TURNSTILE_SITE_KEY=

# Contact info shown on /contact (obfuscated in HTML, decoded in browser)
PUBLIC_CONTACT_EMAIL=
PUBLIC_CONTACT_PHONE=

# Optional: service area line (e.g. "Sydney and surrounds" or "Greater Melbourne and remote")
# Shown on homepage and footer. Leave empty to hide.
PUBLIC_SERVICE_AREA=

# Cloudflare Web Analytics (optional, privacy-friendly)
# Option A: Enable in Cloudflare Dashboard → Workers & Pages → [project] → Metrics → Web Analytics (no code needed)
# Option B: Set these variables for manual control
PUBLIC_ANALYTICS_PROVIDER=cloudflare
PUBLIC_CLOUDFLARE_WEB_ANALYTICS_TOKEN=
