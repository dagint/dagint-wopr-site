---
import Layout from '@/layouts/Layout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog', (p) => p.data.published !== false);
  return posts.map((entry) => ({ params: { slug: entry.slug }, props: { entry } }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();
const site = Astro.site ?? 'https://dagint.com';
const canonical = new URL(`/blog/${entry.slug}`, site);
const blogLd = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: entry.data.title,
  description: entry.data.summary,
  datePublished: entry.data.date.toISOString(),
  dateModified: entry.data.date.toISOString(),
  url: canonical.toString(),
  author: {
    '@type': 'Organization',
    name: 'DaGint Computer Support',
    url: site,
  },
  publisher: {
    '@type': 'Organization',
    name: 'DaGint Computer Support',
    url: site,
    logo: {
      '@type': 'ImageObject',
      url: new URL('/favicon.svg', site).toString(),
    },
  },
  mainEntityOfPage: {
    '@type': 'WebPage',
    '@id': canonical.toString(),
  },
  ...(entry.data.image && { 
    image: {
      '@type': 'ImageObject',
      url: new URL(entry.data.image, site).toString(),
    },
  }),
  ...(entry.data.tags && entry.data.tags.length > 0 && {
    keywords: entry.data.tags.join(', '),
  }),
};
const breadcrumbLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: site.toString() },
    { '@type': 'ListItem', position: 2, name: 'Blog', item: new URL('/blog', site).toString() },
    { '@type': 'ListItem', position: 3, name: entry.data.title },
  ],
};
---

<Layout title={entry.data.title} description={entry.data.summary}>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(blogLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(breadcrumbLd)} />
    <link rel="canonical" href={canonical.toString()} />
  </Fragment>

  <section class="page-banner flex items-end pb-8 sm:pb-10">
    <div class="absolute inset-0 {entry.data.image ? 'opacity-50' : 'opacity-40'}" aria-hidden="true">
      {entry.data.image ? (
        <img
          src={entry.data.image}
          alt=""
          class="w-full h-full object-cover"
          width="1200"
          height="630"
          loading="eager"
          decoding="async"
          fetchpriority="high"
        />
      ) : (
        <div class="w-full h-full wopr-dot-grid"></div>
      )}
    </div>
    <!-- Dark overlay so white text stays readable: stronger when there's an image -->
    <div
      class="absolute inset-0 {entry.data.image
        ? 'bg-gradient-to-t from-[var(--wopr-header)] via-[var(--wopr-header)]/85 to-[var(--wopr-header)]/70'
        : 'bg-gradient-to-t from-[var(--wopr-header)]/95 via-[var(--wopr-header)]/60 to-transparent'}"
      aria-hidden="true"
    />
    <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 w-full">
      <h1 class="page-title-on-banner drop-shadow-md">{entry.data.title}</h1>
      <p class="mt-2 text-white/95 text-sm sm:text-base max-w-2xl drop-shadow-sm">{entry.data.summary}</p>
      <time class="block mt-3 text-xs text-blue-50 drop-shadow-sm" datetime={entry.data.date.toISOString()}>
        {entry.data.date.toLocaleDateString('en-AU', { year: 'numeric', month: 'long', day: 'numeric' })}
      </time>
    </div>
  </section>

  <div class="page-section max-w-3xl mx-auto">
    <nav class="text-sm text-gray-500 mb-6" aria-label="Breadcrumb">
      <ol class="flex items-center gap-1.5">
        <li><a href="/" class="hover:text-[var(--wopr-secondary)] transition-colors">Home</a></li>
        <li aria-hidden="true">/</li>
        <li><a href="/blog" class="hover:text-[var(--wopr-secondary)] transition-colors">Blog</a></li>
        <li aria-hidden="true">/</li>
        <li class="text-gray-700 truncate" aria-current="page">{entry.data.title}</li>
      </ol>
    </nav>
    <article class="blog-post prose prose-slate prose-lg max-w-none prose-headings:font-mono prose-headings:text-[var(--wopr-primary)] prose-headings:scroll-mt-24 prose-a:text-[var(--wopr-secondary)] prose-a:no-underline hover:prose-a:underline prose-a:font-medium prose-p:leading-relaxed prose-ul:my-6 prose-ol:my-6 prose-li:my-1">
      <Content />
    </article>
    <p class="mt-10">
      <a href="/blog" class="text-sm font-medium text-[var(--wopr-secondary)] hover:text-[var(--wopr-primary)] hover:underline">← Back to blog</a>
      <span class="mx-2 text-gray-500">·</span>
      <a href="/contact" class="text-sm font-medium text-[var(--wopr-secondary)] hover:text-[var(--wopr-primary)] hover:underline">Get in touch</a>
    </p>
  </div>
</Layout>
